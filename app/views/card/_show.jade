div.bangcard
  div.card-container
    div.card-title
      {{card.title}}
      span.card-suit 
        {{card.suit}}
        {{card.value}}
    div.card-image
      img( src="{{card.image}}" )
    div.card-content {{card.content}}

